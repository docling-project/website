[project]
name = "vercel-fastapi-starter"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.129.0",
  "python-jsx>=0.4.0",
  "uvicorn>=0.40.0",
]

[tool.setuptools.packages.find]
include = ["website*"]
namespaces = true

[tool.uv]
package = true
